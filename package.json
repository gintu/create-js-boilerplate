{
  "name": "create-js-boilerplate",
  "version": "1.0.0",
  "description": "boilerplate code for vanila-js",
  "bin": {
    "create-js-boilerplate": "./bin/index.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "rm -rf ./development && rm -rf ./.parcel-cache && parcel public/index.html --dist-dir development -p 3000",
    "build": "parcel build public/index.html --dist-dir dist --public-url ./"
  },
  "author": "gintu tom",
  "license": "MIT",
  "devDependencies": {
    "@babel/core": "^7.14.3",
    "@babel/plugin-transform-runtime": "^7.14.3",
    "@babel/runtime-corejs2": "^7.14.0",
    "parcel": "^2.0.0-beta.3.1"
  }
}
